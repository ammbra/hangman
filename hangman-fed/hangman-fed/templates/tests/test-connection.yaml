apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hangman-fed.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "hangman-fed.name" . }}
    helm.sh/chart: {{ include "hangman-fed.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "hangman-fed.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
